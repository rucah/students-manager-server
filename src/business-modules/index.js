//const auth = require('./auth-module');
const clusters = require('./clusters-module');
const students = require('./students-module');
const grades = require('./grades-module');


module.exports = {
    // auth,
    clusters,
    students,
    grades
}